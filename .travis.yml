sudo: required

services:
  - docker

branches:
  only:
    - master
    - /.*-alpha.*/

language: ruby
bundler_args: --without integration
rvm:
  - 2.2.2
script:
  - bundle exec rake lint
  - bundle exec rake kitchen:suite:all
